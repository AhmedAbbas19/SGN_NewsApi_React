import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  form: {
    maxWidth: 450,
    margin: "0 auto",
  },
  container: {
    marginTop: 42,
  },
  submitBtn: {
    marginTop: theme.spacing(2),
  },
}));
